import 'package:flutter/material.dart';

class ScoreUpdate extends StatelessWidget {
  const ScoreUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Score Update");
  }
}
